apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nvmesh-concatenated
spec:
  accessModes:
    - ReadWriteMany
  volumeMode: Block
  resources:
    requests:
      storage: 5Gi
  storageClassName: nvmesh-concatenated
