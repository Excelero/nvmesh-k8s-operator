TODO: Add SCC to Scheme

securityv1 "github.com/openshift/api/security/v1"
//[...]
//Add OpenShift security to scheme
	if err := securityv1.AddToScheme(mgr.GetScheme()); err != nil {
		log.Error(err, "")
		os.Exit(1)
	}





TODO: Add watches for child objects (Statefulsets, daemonsets , etc.. )
//[...]
func add(mgr manager.Manager, r reconcile.Reconciler) error {
//[...]
err = c.Watch(&source.Kind{Type: &securityv1.SecurityContextConstraints{}}, &handler.EnqueueRequestForOwner{
		IsController: true,
		OwnerType:    &sccv1alpha1.SccOp{},
	})
	if err != nil {
		return err
	}
//[...]


TODO: TOMAs persistent cache is being deleted everytime toma pods are restarted
* toma cache - nvmeibt_read_config.c:2684 [nvmeibt_read_persisted_config]  No previous config file /var/opt/NVMesh/toma/toma_config.csv_bufs   
* check what the consequences are and try to find a solution (mount a hostPath volume?)


TODO: Test restarting each component in the solution   


TODO: decide where to keep the yamls to deploy the components ?
  * only in the operator ?
  * management yamls, csi yamls, core yamls, and there are the operator yamls (rbac and deployment to deploy the operator)


TODO: upload nvmesh-io-test image to dockerhub

TODO: TOMA crashes ahen treace daemon logs too long, requires manual clean up of /var/log/NVMesh